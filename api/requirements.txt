# Vercel Python serverless dependencies (local-first; no paid search APIs)
Django>=5.0
asgiref>=3.7
numpy>=1.26
scipy>=1.11
faiss-cpu>=1.7.4
pydantic>=2.0
pyyaml>=6.0
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
chardet>=5.2
vercel-wsgi>=0.2.0

# Note: torch / transformers / sentence-transformers are heavy for Vercel
# serverless. Prefer a dedicated host for full local BGE + Qwen inference.
# Lightweight CPU paths still work with numpy/faiss + ManualTuner hooks.
