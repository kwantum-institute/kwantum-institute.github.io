# Lightweight deps for Vercel Django service (no torch / transformers / scipy).
# Full local ML stack remains in requirements.txt.
# Keep under Vercel serverless function size limit (~225 MB compressed).
Django>=5.0,<6
djangorestframework>=3.15
django-cors-headers>=4.3
Pillow>=10.1.0
python-decouple>=3.8
asgiref>=3.7
numpy>=1.26
pydantic>=2.0
pyyaml>=6.0
httpx>=0.27
beautifulsoup4>=4.12
