# Core web framework
Django>=5.0
asgiref>=3.7

# Validation and config
pydantic>=2.0
pyyaml>=6.0

# Numerical / embeddings (local, no API)
numpy>=1.26
scipy>=1.11
faiss-cpu>=1.7.4

# Local NLP + LLM stack (BGE + Qwen on-device)
torch>=2.1
transformers>=4.40
sentence-transformers>=3.0
accelerate>=0.27
safetensors>=0.4
huggingface-hub>=0.20
tokenizers>=0.15
sentencepiece>=0.1.99
einops>=0.7

# Local document / corpus helpers (no paid search APIs)
beautifulsoup4>=4.12
lxml>=5.0
chardet>=5.2

# Optional HTTP only for local OpenAI-compatible endpoints (vLLM/SGLang)
# Not required when using in-process transformers inference
httpx>=0.27

# Testing
pytest>=8.0
pytest-django>=4.8
