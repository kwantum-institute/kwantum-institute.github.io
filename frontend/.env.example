# Copy this file to .env only if you need non-default API hosts.
# Defaults (recommended): same-origin /api via Vite proxy locally and Vercel rewrites in production.
# VITE_API_BASE_URL=/api
# VITE_GRAPHRAG_API_URL=/api/graphrag

